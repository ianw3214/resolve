"use strict";

let healthSystem = {
    generate: function(health = 3) {
        return {
            health: 3
        }
    },
    update: function(entities, delta) {
        // TODO: Maybe something here
    }
}